<meta charset='utf-8' />
<script src="http://code.jquery.com/jquery-1.9.0.min.js"></script>
<script src="http://code.jquery.com/jquery-migrate-1.1.0.min.js"></script>
<link rel='stylesheet' href='/stylesheets/style.css' />
<script src="/socket.io/socket.io.js"></script>
<script>
mainsocket = io.connect('http://'+location.host+':'+80);
/*
//mainsocket = io.connect('http://'+location.host+':'+port);
//mainsocket = io.connect("http://<%= wshost %>/");
mainsocket.on('announce',function(data){
    var dat = JSON.parse(data);
    var $cont;
    switch(dat.mode) {
    case 'new room':
        $cont = $("<p>").html(dat.user+"さんが"+"<a href='/room?number="+dat.room+">"+dat.title+"を開設しました。");
        $("#announce").append($cont);
        var width = $(this).parent().css('width');
        $cont.css({left:width+'px'});
        $cont.animate({left:'0px'},500);
        setTimeout(function(){$cont.animate({left:'-'+width+'px'},500);},500);
        break;
    default: break;
    }
});
*/
</script>
